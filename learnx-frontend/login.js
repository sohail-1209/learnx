function handleLiveSession() {
  // Add functionality for live session page
  console.log("Handling live session...");
}

// Call the function when the DOM is fully loaded
document.addEventListener("DOMContentLoaded", handleLiveSession);